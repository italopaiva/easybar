<p id="notice"><%= notice %></p>

<h1>Orders</h1>

<table>
  <thead>
    <tr>
      <th>Content</th>
      <th>User</th>
      <th>Table</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @orders.each do |order| %>
      <tr>
        <td><%= order.content %></td>
        <td><%= order.user %></td>
        <td><%= order.table %></td>
        <td><%= link_to 'Show', user_order_path(@user, order) %></td>
        <td><%= link_to 'Edit', edit_user_order_path(@user, order) %></td>
        <td><%= link_to 'Destroy', user_order_path(@user, order), method: :delete, data: { confirm: 'Você tem certeza?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Order', new_user_order_path %>
